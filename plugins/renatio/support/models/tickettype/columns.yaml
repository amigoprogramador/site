# ===================================
#  List Column Definitions
# ===================================

columns:

    name:
        label: renatio.support::lang.field.name
        searchable: true

    description:
        label: renatio.support::lang.field.description
        searchable: true

    is_active:
        label: renatio.support::lang.field.is_active
        type: switch

    created_at:
        label: renatio.support::lang.field.created_at
        type: datetime

    updated_at:
        label: renatio.support::lang.field.updated_at
        type: datetime
        invisible: true